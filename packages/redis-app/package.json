{
  "name": "redis-app",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "docker-up": "docker-compose up -d",
    "docker-stop": "docker-compose stop",
    "docker-down": "docker-compose down",
    "docker-restart": "docker-compose restart",
    "cli": "./node_modules/.bin/rdcli"
  },
  "devDependencies": {
    "redis-cli": "^2.1.0"
  }
}
