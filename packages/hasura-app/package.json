{
  "name": "hasura-app",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "docker-up": "docker-compose up -d",
    "docker-stop": "docker-compose stop",
    "docker-down": "docker-compose down",
    "docker-restart": "docker-compose restart",
    "cli": "./node_modules/.bin/hasura"
  },
  "devDependencies": {
    "hasura-cli": "^2.0.3"
  }
}
