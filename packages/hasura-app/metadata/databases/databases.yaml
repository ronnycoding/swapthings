- name: main
  kind: postgres
  configuration:
    connection_info:
      database_url: postgres://user:example@postgres:5432/hasura-app
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include main/tables/tables.yaml"
